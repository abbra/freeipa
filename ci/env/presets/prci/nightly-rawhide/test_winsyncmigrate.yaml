# Generated by `freeipa-env migrate` from ../../ipatests/prci_definitions/nightly_rawhide.yaml
# PRCI job: fedora-rawhide/test_winsyncmigrate  (class: RunADTests)
# PRCI topology: ad_master (cpu 4, memory 8096)
# PRCI timeout: 4800s (informational; freeipa-env has no timeout field)

provider: podman
name: test_winsyncmigrate
domain: ipa.test
image: freeipa-next
hosts:
- role: master
  name: master1
- role: ad
  name: ad1.ad.test
  address: 192.0.2.101
  user: root
resources:
  master:
    memory: 8000m
    memory-swap: 16000m
run:
  mode: base
  setup_dns: true
  setup_kra: true
  tests:
  - test_integration/test_winsyncmigrate.py

# --- Active Directory hosts (external): PRCI provisions these AD DCs itself; freeipa-env does not.
# Point each 'address' at a reachable AD DC and set 'name' to its real FQDN
# before `freeipa-env up`. The IPA containers resolve the AD hosts via /etc/hosts.
# ad: ad1.ad.test (192.0.2.101) -- EDIT ME
