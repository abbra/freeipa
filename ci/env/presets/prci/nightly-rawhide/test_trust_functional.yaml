# Generated by `freeipa-env migrate` from ../../ipatests/prci_definitions/nightly_rawhide.yaml
# PRCI job: fedora-rawhide/test_trust_functional  (class: RunADTests)
# PRCI topology: adroot_adchild_adtree_master_2client (cpu 9, memory 15000)
# PRCI timeout: 14400s (informational; freeipa-env has no timeout field)

provider: podman
name: test_trust_functional
domain: ipa.test
image: freeipa-next
hosts:
- role: master
  name: master1
- role: client
  name: client1
- role: client
  name: client2
- role: ad
  name: ad1.ad.test
  address: 192.0.2.101
  user: root
- role: ad_subdomain
  name: ad1.adchild.ad.test
  address: 192.0.2.102
  user: root
- role: ad_treedomain
  name: ad1.adtree.example
  address: 192.0.2.103
  user: root
resources:
  master:
    memory: 5000m
    memory-swap: 10000m
  client:
    memory: 5000m
    memory-swap: 10000m
run:
  mode: integration
  setup_dns: true
  setup_kra: true
  tests:
  - test_integration/test_trust_functional.py

# --- Active Directory hosts (external): PRCI provisions these AD DCs itself; freeipa-env does not.
# Point each 'address' at a reachable AD DC and set 'name' to its real FQDN
# before `freeipa-env up`. The IPA containers resolve the AD hosts via /etc/hosts.
# ad: ad1.ad.test (192.0.2.101) -- EDIT ME
# ad_subdomain: ad1.adchild.ad.test (192.0.2.102) -- EDIT ME
# ad_treedomain: ad1.adtree.example (192.0.2.103) -- EDIT ME
