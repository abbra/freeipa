# Generated by `freeipa-env migrate` from ../../ipatests/prci_definitions/nightly_latest_testing_selinux.yaml
# PRCI job: testing-fedora/test_webui_subid  (class: RunWebuiTests)
# PRCI topology: ipaserver (cpu 2, memory 2750)
# PRCI timeout: 2400s (informational; freeipa-env has no timeout field)
# NOTE: PRCI RunWebuiTests needs a browser + selenium on the test host,
#       which freeipa-ci/full does not include; this preset will not pass as-is.

provider: podman
name: test_webui_subid
domain: ipa.test
image: freeipa-ci/full:44
hosts:
- role: master
  name: master1
resources:
  master:
    memory: 2700m
    memory-swap: 5400m
run:
  mode: base
  setup_dns: true
  setup_kra: true
  tests:
  - test_webui/test_subid.py
