# Generated by `freeipa-env migrate` from ../../ipatests/prci_definitions/nightly_latest_testing_selinux.yaml
# PRCI job: testing-fedora/test_webui_rbac  (class: RunWebuiTests)
# PRCI topology: ipaserver (cpu 2, memory 2750)
# PRCI timeout: 3600s (informational; freeipa-env has no timeout field)
# NOTE: PRCI RunWebuiTests needs a browser + selenium on the test host,
#       which freeipa-ci/full does not include; this preset will not pass as-is.

provider: podman
name: test_webui_rbac
domain: ipa.test
image: freeipa-current
hosts:
- role: master
  name: master1
resources:
  master:
    memory: 2700m
    memory-swap: 5400m
run:
  mode: base
  setup_dns: true
  setup_kra: true
  tests:
  - test_webui/test_delegation.py
  - test_webui/test_rbac.py
  - test_webui/test_selfservice.py
