# Generated by `freeipa-env migrate` from ../../ipatests/prci_definitions/nightly_previous.yaml
# PRCI job: fedora-previous/test_webui_host_net_groups  (class: RunWebuiTests)
# PRCI topology: ipaserver (cpu 2, memory 2750)
# PRCI timeout: 3600s (informational; freeipa-env has no timeout field)
# NOTE: PRCI RunWebuiTests needs a browser + selenium on the test host,
#       which freeipa-ci/full does not include; this preset will not pass as-is.

provider: podman
name: test_webui_host_net_groups
domain: ipa.test
image: freeipa-ci/full:44
hosts:
- role: master
  name: master1
resources:
  master:
    memory: 2700m
    memory-swap: 5400m
run:
  mode: base
  setup_dns: true
  setup_kra: true
  tests:
  - test_webui/test_hostgroup.py
  - test_webui/test_netgroup.py
