# Generated by `freeipa-env migrate` from ../../ipatests/prci_definitions/nightly_latest.yaml
# PRCI job: fedora-latest/test_32BitIdranges  (class: RunADTests)
# PRCI topology: ad_master_1repl_1client (cpu 6, memory 12096)
# PRCI timeout: 7200s (informational; freeipa-env has no timeout field)

provider: podman
name: test_32bitidranges
domain: ipa.test
image: freeipa-ci/full:44
hosts:
- role: master
  name: master1
- role: replica
  name: replica1
- role: client
  name: client1
- role: ad
  name: ad1.ad.test
  address: 192.0.2.101
  user: root
resources:
  master:
    memory: 4000m
    memory-swap: 8000m
  replica:
    memory: 4000m
    memory-swap: 8000m
  client:
    memory: 4000m
    memory-swap: 8000m
run:
  mode: integration
  setup_dns: true
  setup_kra: true
  tests:
  - test_integration/test_32bit_idranges.py

# --- Active Directory hosts (external): PRCI provisions these AD DCs itself; freeipa-env does not.
# Point each 'address' at a reachable AD DC and set 'name' to its real FQDN
# before `freeipa-env up`. The IPA containers resolve the AD hosts via /etc/hosts.
# ad: ad1.ad.test (192.0.2.101) -- EDIT ME
