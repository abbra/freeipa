# Generated by `freeipa-env migrate` from ../../ipatests/prci_definitions/nightly_latest.yaml
# PRCI job: fedora-latest/test_webui_sysaccount  (class: RunWebuiTests)
# PRCI topology: ipaserver (cpu 2, memory 2750)
# PRCI timeout: 2400s (informational; freeipa-env has no timeout field)
# NOTE: PRCI RunWebuiTests needs a browser + selenium on the test host,
#       which freeipa-ci/full does not include; this preset will not pass as-is.

provider: podman
name: test_webui_sysaccount
domain: ipa.test
image: freeipa-current
hosts:
- role: master
  name: master1
resources:
  master:
    memory: 2700m
    memory-swap: 5400m
run:
  mode: base
  setup_dns: true
  setup_kra: true
  tests:
  - test_webui/test_sysaccount.py
