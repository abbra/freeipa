# Ordered run queue for the PRCI definition `temp_commit.yaml`
# (job order = PRCI definition order; generated by
# `freeipa-env queue generate`, like the presets it references).
name: prci-temp-commit
jobs:
  # fedora-latest/build: skipped (class Build: replaced by the freeipa-ci image pipeline)
  - presets/prci/temp-commit/temp_commit.yaml  # fedora-latest/temp_commit
