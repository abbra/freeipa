specfile_path: freeipa.spec
actions:
  post-upstream-clone:
    - "make -C .copr srpm outdir=."

# name in upstream package repository/registry (e.g. in PyPI)
upstream_package_name: freeipa
# downstream (Fedora) RPM package name
downstream_package_name: freeipa
upstream_project_url: https://github.com/abbra/freeipa

jobs:
- job: copr_build
  trigger: commit
  branch: wip-ipa-ipa-trust
  owner: abbra
  project: wip-ipa-trust
  targets: [fedora-39-x86_64]

